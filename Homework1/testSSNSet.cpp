#include <iostream>
#include <cassert>
#include "SSNSet.h"

int main()
{
	/*SSNSet s;
	assert(s.size() == 0);
	s.add(2);
	s.add(1);
	s.add(124);
	s.add(76);
	s.add(2);
	s.print();
	assert(s.size() == 4);

	SSNSet b;      // b can hold at most 5 distinct items
	SSNSet c;         // c can hold at most DEFAULT_MAX_ITEMS distinct items
	ItemType v[6] = { 5500,69,99,13,85,50 };

	// No failures inserting 5 distinct items into b
	for (int k = 0; k < 5; k++)
		assert(b.add(v[k]));

	//copy constructor working as intended
	SSNSet d = b;
	d.print();
	//assignment operator works as intended
	c = d;
	c.print();
	c.add(12);
	c.print();
	assert(c.size() == 6);

	cerr << "All tests passed!" << endl;
	*/
	return 0;
}